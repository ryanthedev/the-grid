### [high] Every space switch routes spaceIDReassigned, migrating/destroying GridState
LOC: grid-server/Sources/GridServer/StateManager.swift:1762
MECH: WorkspaceObserver.spaceChanged (WorkspaceObserver.swift:132-142) routes focusChanged(trigger:.spaceSwitched) -> StateManager.handle case .focusChanged (StateManager.swift:231-233) calls handleSpaceChanged() -> StateManager.swift:1744-1751 detects ANY display whose currentSpaceID changed (true for every normal desktop switch, not just macOS space-ID reassignment) and at StateManager.swift:1762-1769 routes .spaceIDReassigned(old,new) -> GridReconciler.handle (GridReconciler.swift:432-439, processe
SYMPTOM: After switching between two desktops that both have layouts, one desktop's tiling is gone ('no layout applied') and the other suddenly has the wrong layout/window assignments; focus commands target windows that live on the other space.
FIX: Only route spaceIDReassigned when the old space ID no longer exists in the refreshed state.spaces (true reassignment); plain switches should route a dedicated spaceActivated event.

### [high] SIGPIPE on client-socket send kills entire server
LOC: grid-server/Sources/GridServer/SocketServer.swift:240
MECH: SocketServer.sendMessage (SocketServer.swift:233-254) writes replies with a raw BSD send(socket, ptr, count, 0) (SocketServer.swift:240-242). The only socket option ever set is SO_REUSEADDR on the listening socket (:33-34); accepted client sockets never get SO_NOSIGPIPE, macOS has no MSG_NOSIGNAL, and SIGPIPE is never set to SIG_IGN. Instead, CrashReporter.install() registers SIGPIPE as a FATAL signal (CrashReporter.swift:78 installSignal(SIGPIPE)) whose handler calls Darwin._exit(128 &+ sig) (C
SYMPTOM: Entire grid-server process dies (tiling, hotkeys, borders all gone until restart) the moment any client disconnects between request and reply or mid-broadcast. Log shows srv.fatal SIGPIPE then srv.prev_exit on next start.
FIX: Set SO_NOSIGPIPE on every accepted client socket (and/or signal(SIGPIPE, SIG_IGN)), letting send() return EPIPE which the existing sock.err branch handles; drop SIGPIPE from CrashReporter's fatal-signal list.

### [high] GridReconciler is an unsynchronized plain class — suppressionDepth/fencedWindows/pendingLaunchTarget mutated from 4+ concurrent contexts; 7x duplicate sweep processing observed
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:21
MECH: GridReconciler.swift:21 declares 'class GridReconciler: StateEventHandler' (not an actor; Package.swift is swift-tools 5.9, no strict concurrency, so data races compile silently). Its handle() (:414) is a nonisolated async method, so when the EventRouter actor awaits it (EventRouter.swift:205) it runs on the global concurrent executor. Mutable fields are touched from: (1) EventRouter dispatch tasks — handle() reads suppressReconciliation (:442) and mutates fencedWindows via isWindowFenced lazy e
SYMPTOM: Duplicate window-create processing (redundant layout applies / border-sync storms with visible flicker) when a rejected window matures; latent risk of permanent reconciliation freeze (new windows never tile, borders stop following focus) or an EXC_BA
FIX: Convert GridReconciler to an actor (or confine it to a serial executor) — all callers already await its methods — and make the sweep timer skip ticks while a sweep task is still running.

### [medium] windowCreated/windowDestroyed events silently dropped during suppression are never replayed — window permanently untiled, dead windows linger up to 30s
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:442
MECH: Every user command runs inside GridReconciler.executeAction (GridReconciler.swift:91-129), which increments suppressionDepth; beginAction/endAction sessions last arbitrarily long — nudge mode holds suppression across keystrokes (GridCommandRouter.swift:790-861) and picker.result holds one across the picker flow (PickerManager.swift:198-272; 64x action.begin label picker.result in logs). While suppressed, handle() silently drops all non-focus, non-pending events at GridReconciler.swift:441-444 ('
SYMPTOM: A window opened while a hotkey command, layout apply, nudge session, or the picker is in flight permanently floats untiled — no border, invisible to focus navigation — until the layout is manually reapplied. A window closed during suppression leaves 
FIX: Queue (don't drop) windowCreated/windowDestroyed events received during suppression and replay them when suppressionDepth returns to 0; alternatively have the depth-0 sweep also scan for tileable windows absent from every GridState cell and remove tr

### [medium] focusSweep reverts focus mid-command: never consults the fence, move commands run unsuppressed, and the suppression check is TOCTOU
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:339
MECH: Three gaps in one path. (1) No fence check: the 300ms focusSweep (GridReconciler.swift:41 sweepInterval=0.3, started :317-335) compares StateManager's cached OS focus against GridState focus (:344-352) and corrects GridState + borders on mismatch (:360-371), but never consults fencedWindows — the fence (:63, :827-839) only guards handleFocusChanged. (2) Moves unsuppressed: GridCommandRouter.swift:183-184 'case "window": return try await handleWindow(parsed)' has no executeAction wrapper (unlike 
SYMPTOM: Focused border flicks/ping-pongs back to the previously focused window ~0-300ms after a focus or move command; with rapid sequential moves, a follow-up command reads the reverted focusedCell and moves/focuses the wrong window; sweep.correct entries a
FIX: Have focusSweep skip windows present in fencedWindows (both osWindowID and the grid-focused wid), wrap 'window' commands in executeAction, and re-check suppression (or a generation counter incremented by beginAction) immediately before the setFocus/u

### [medium] Fence is not refcounted — overlapping moves of the same window strip each other's fence
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:179
MECH: Commands are fully concurrent: each CLI/hotkey invocation is a separate socket connection handled on a concurrent queue (SocketServer.swift:10 'clientQueue = DispatchQueue(label: "com.thegrid.client", attributes: .concurrent)'), and GridCommandRouter.dispatch is invoked per-request with no serialization. A same-space move acquires a per-window fence (GridWindowMove.swift:255 acquireFence) -> mutates GridState (:258-261) -> applies AX placements (:322) -> AX-focuses the window (:331) -> syncs bor
SYMPTOM: During rapid repeated window moves (held hotkey), focus and the focused border ping-pong between the moved window and the previously focused window; occasionally the next move grabs the wrong window because GridState focus was reverted mid-sequence.
FIX: Make fencedWindows a refcount (or per-acquisition token) so releaseFence only removes the entry when the count reaches zero; alternatively serialize move commands per window.

### [medium] WindowManipulator sole-AX-window fallback is unguarded — manipulations land on the wrong window (residual gap after 1cf354e)
LOC: grid-server/Sources/GridServer/WindowManipulator.swift:78
MECH: Commit 1cf354e added the shouldUseSoleWindowFallback guard ONLY to StateManager.getAXProperties (StateManager.swift:586-596 checks _AXUIElementGetWindow(windows[0], &soleWindowID) and refuses when the sole window resolves to a DIFFERENT CG ID than the queried phantom). WindowManipulator.getAXElement (WindowManipulator.swift:57-85) still has the original unguarded fallback: after failing the ID match loop, 'if windows.count == 1 { return windows[0] }' (:78-80, verified in source) with no resoluti
SYMPTOM: A real window suddenly jumps/resizes to a cell it was never assigned to when the reconciler or layout-apply touches a phantom ID; focus raise lands on the wrong same-app window; state frame recorded for a phantom while the real window's state frame g
FIX: Apply the same shouldUseSoleWindowFallback(resolved:soleWindowID:queriedID:) guard in WindowManipulator.getAXElement before returning windows[0], and log when the fallback fires.

### [medium] Cross-display move ignores moveWindowToSpace failure and mutates state anyway
LOC: grid-server/Sources/GridServer/Grid/GridWindowMove.swift:432
MECH: GridWindowMove.moveWindowCrossDisplay discards the Bool result of the space move: GridWindowMove.swift:432 'let _ = windowManipulator.moveWindowToSpace(windowID:spaceID:)'. WindowManipulator.moveWindowToSpace (WindowManipulator.swift:336-422) returns false on several real paths: target space is fullscreen (err.space), MSS move fails (mss.fail), post-move verification mismatch (err.verify), and notably the MSS-unavailable SLS fallback (:393-401) which returns 'newSpace == spaceID' with NO log on 
SYMPTOM: Move-to-display hotkey: the window does not move, but focus jumps and the active-space override points at the wrong space; borders highlight an empty cell on the target display; the window appears lost from the grid until a later command's displaced-
FIX: Check the Bool from moveWindowToSpace; on false, log, release the fence, and abort before any GridState mutation (or roll back). Also add the missing err.verify log to the SLS-fallback branch in WindowManipulator.swift:401.

### [high] focusWindowByID verifies against stale cached focus (single Task.yield), causing spurious mismatch/retry and wrong 'actual' propagation
LOC: grid-server/Sources/GridServer/Grid/GridFocus.swift:414
MECH: GridFocus.focusWindowByID calls windowController.focusWindow (GridFocus.swift:402) which is WindowManipulator's port conformance (WindowManipulator.swift:572-574) — it does NOT call stateManager.setFocusedWindow (only the unused context variant at WindowManipulator.swift:513-519 does). Verification then does a single 'await Task.yield()' (GridFocus.swift:414) and reads cached postState.metadata.focusedWindowID (:418-419). That cache is only updated when the OS focus notification travels AX-callb
SYMPTOM: Doubled window raises on focus commands; cycle-focus occasionally jumping two windows or briefly raising several windows; CLI/borders momentarily highlighting a different window than the one actually focused.
FIX: Have the WindowController port update StateManager focus synchronously on success (like the context variant), or verify via a direct AX focused-window query instead of the event-fed cache.

### [high] Directional focus dead-ends on empty cells instead of skipping them (noWindowsInCell spam on 'notify' cell)
LOC: grid-server/Sources/GridServer/Grid/GridFocus.swift:185
MECH: moveFocus (GridFocus.swift:92) collects adjacency candidates (:136 adjacentMap[direction]) without filtering for cells that contain windows. pickClosestCell (:185, impl :766-792) selects purely by center distance — emptiness is never considered. The winner goes to focusCellByID (:188), which prunes dead windows and then throws GridFocusError.noWindowsInCell(cellID) when the cell is empty (:324-326). There is no retry with the next candidate, no skip-over to the cell beyond, and no wrap fallback 
SYMPTOM: Pressing focus-left/right does nothing when the nearest adjacent cell is empty; user retries repeatedly (log shows 3 failures within 1s at ts 1781151526-1527). Cells beyond the empty cell are unreachable in that direction.
FIX: Filter direction candidates (and wrap targets) to non-empty cells, falling through to the next-closest candidate / next cell in that direction; optionally allow focusing an empty cell as a no-window focus.

### [medium] Per-notification unstructured Task spawn loses event ordering — focus inversions and destroy-before-create for short-lived windows
LOC: grid-server/Sources/GridServer/ApplicationObserver.swift:342
MECH: AX notifications arrive in order on the main run loop but axNotificationCallback spawns an independent unstructured Task per notification (ApplicationObserver.swift:342-344 'Task { await appObserver.handleNotification(...) }'); WorkspaceObserver.applicationActivated does the same (WorkspaceObserver.swift:186-197, including a synchronous getFocusedWindowID AX query of variable latency inside the Task), and StateManager's poll routes its create/destroy batches in separate detached Tasks (StateMana
SYMPTOM: Focus border flips back to the previously focused window while the user is already typing in the new one (enforced by the sweep for up to 300ms cycles); ghost window briefly tracked/tiled after a transient dialog closes; spurious win.created after wi
FIX: Funnel AX/workspace notifications through a single ordered pipeline (AsyncStream consumed by one task, or a serial actor queue) instead of one detached Task per callback, and stamp focus events with a monotonic sequence so applyWindowFocus rejects ol

### [low] handleWindowMinimized removes the window from the focused display's space instead of the window's tracked space — minimized window keeps its cell slot
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:1052
MECH: On .windowMinimized, the reconciler resolves 'currentSpaceID = await findCurrentSpaceIDAsync()' — metadata.activeSpaceID, i.e. the focused display's active space (GridReconciler.swift:1052, :1263-1275) — and calls gridState.removeWindow on THAT space (:1054). If the minimized window is tracked in a different space (yellow-button minimize on a second display's space, CLI/MCP window_minimize against any wid, or the event arriving after a space switch / stale activeSpaceID), GridState.removeWindow 
SYMPTOM: After minimizing a window (especially on a non-focused display or via CLI), its cell still reserves a split for it: remaining windows stay half-width, cell ops compute splits for an invisible window, and focus cycle can target a Dock-minimized window
FIX: Use gridState.findSpaceContaining(windowID:) (as handleFocusChanged does at :848) to resolve the space to remove from, falling back to removeWindowFromAllSpaces.

### [low] pid-less pendingLaunchTarget claimable by any app's window — and a silently failed picker launch leaves it armed for 15s
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:708
MECH: After a picker selection, PickerManager.handleResult arms the launch target BEFORE executing the action (PickerManager.swift:221-228 capturedOnLaunch(action) -> GridCommandRouter.swift:616-629 setPendingLaunchTarget with pid:nil); pid stays nil until NSWorkspace's launch completion fires updatePendingLaunchTargetPID (GridReconciler.swift:197-201). During that gap, handle() routes EVERY .windowCreated to handlePendingLaunchWindow even during suppression (:423-428), and the rejectedWindowSweep (:4
SYMPTOM: Picker places and focuses the wrong window in the chosen cell while the picked app lands elsewhere unfocused; after picking an app that fails to launch, a window opened by any other means within 15 seconds gets teleported into the picker's target cel
FIX: Buffer candidate windows until the launch PID arrives (or match on the picked app's bundle ID) instead of letting the first tileable window claim a pid-less target; in ActionExecutor .openApp add an else branch that logs and clears the pending target

### [medium] pendingLaunchTarget double-consumption: check-then-consume spans multiple awaits between the sweep and event paths
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:404
MECH: Two independent paths call handlePendingLaunchWindow: the EventRouter windowCreated path (GridReconciler.swift:423-428, checked BEFORE the suppression guard) and the 300ms timer's rejectedWindowSweep (:404-408) when a previously-rejected phantom becomes tileable. handlePendingLaunchWindow reads pendingLaunchTarget at :698, then performs multiple awaits (getState :730, layout check :791) before consuming the target at :799 ('pendingLaunchTarget = nil') and prepending+focusing (:800-807). Both inv
SYMPTOM: After launching an app from the picker, two windows of that app (one being the recovered phantom) both land in the chosen cell, or focus jumps twice; the picker target cell ends up with an unexpected extra window.
FIX: Consume (or mark in-flight) the pending target before the first await in handlePendingLaunchWindow, restoring it on the skip paths.

### [medium] sweepDisplacedWindows migrates a window's cell assignment but never repositions it
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:1426
MECH: When sweepDisplacedWindows (GridReconciler.swift:1394-1446) detects a window whose OS space differs from its tracked space, it does 'await gridState.removeWindow(wid, fromSpace: trackedSpaceID); await gridState.assignWindow(wid, toCellID: targetCell, inSpace: targetSpaceID)' (:1425-1426) and then only syncBordersForSpace (:1439-1445). It never calls gridApply.applyCellLayout for either the source or the target cell, unlike every other assignment path (locked assign at :642/:663; picker path at :
SYMPTOM: After a window moves across spaces (or gets force-migrated by the deriveSpaceFromDisplay bug), it is logically a cell member — border drawn, focus navigation includes it — but physically sits at its old frame overlapping other cells; co-cell windows 
FIX: After migrating, call applyCellLayout for the target cell (and the vacated source cell) so geometry matches the new assignments.

### [low] Locked-rule window assigned to a cell on an inactive space is never placed there and gets bounced back by the displacement sweep
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:660
MECH: When a locked app's window is created and the locked cell only exists on another space, handleWindowCreated assigns it there in GridState (GridReconciler.swift:660) but findDisplayUUIDForSpace (:1309-1316) only matches a display whose currentSpaceID equals that space. If the locked cell's space is NOT currently visible on any display, displayUUID is nil, so the 'if let displayUUID' block (:662-665) skips applyCellLayout — the window is never positioned or moved to the target space, while reconci
SYMPTOM: An app with a locked cell rule launches while that cell's space is in the background: its window stays on the wrong space in an arbitrary cell, never reaching the reserved cell.
FIX: When the locked cell's space is not the current space of any display, move the window to that space via windowController.moveWindowToSpace before assigning, or defer the locked assignment until the space becomes active.

### [medium] Reconciler space-change handling is dead code: FocusState.previous* never populated and spaceSwitched events carry nil windowID
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:866
MECH: GridReconciler.handleFocusChanged detects a space change via 'focusState.previousSpaceID != nil && previousSpaceID != spaceID && spaceID != 0' (GridReconciler.swift:866-868) and only then calls its handleSpaceChanged border-sync (:871, defined :895-908). But FocusState.previousSpaceID/previousWindowID are never set anywhere in the codebase — every constructor call (ApplicationObserver.swift:176-191, WorkspaceObserver.swift:135-139 and 190-195) uses the nil defaults from the convenience init (Eve
SYMPTOM: After switching spaces, borders/focus highlight are stale or missing until the next focus change or a 300ms sweep mismatch — and would be permanently unsynced if the spurious migration path were fixed.
FIX: Handle the spaceSwitched trigger explicitly in handleFocusChanged (don't require windowID), or populate FocusState.previous* fields at the event source.

### [low] createObserver registration TOCTOU — duplicate AXObservers and dangling refcon after wake rebuild
LOC: grid-server/Sources/GridServer/StateManager.swift:1196
MECH: createObserver guards 'applicationObservers[pid] == nil' (StateManager.swift:1196) but the observer is only inserted into the dictionary later, after a fire-and-forget 'Task { @MainActor in if observer.observe(...) { await self.addApplicationObserver(...) } }' completes (:1202-1206 -> :1210-1212). Any second createObserver for the same pid during that async window passes the nil guard and creates a second live AXObserver. Realistic trigger: handleSystemWoke -> rebuildAXObservers (:2083-2101) spa
SYMPTOM: Doubled AX events (duplicate ev.win.create / ev.focus per real event) after wake; rare main-thread crash in AX callback shortly after wake or app-launch storms.
FIX: Reserve the dict slot synchronously (insert a placeholder) before spawning the MainActor Task, and stopObserving() any observer being replaced in addApplicationObserver.

### [low] StateManager actor blocked by synchronous AX IPC — no AXUIElementSetMessagingTimeout anywhere
LOC: grid-server/Sources/GridServer/StateManager.swift:1356
MECH: Every poll tick, updateWindowFromPoll re-queries AX properties for EVERY window whose role is nil (StateManager.swift:1353-1367) synchronously on the StateManager actor. getAXProperties (:549-599) issues several blocking AXUIElementCopyAttributeValue/_AXUIElementGetWindow IPC calls; the codebase never calls AXUIElementSetMessagingTimeout (grep: 0 hits), so each call to an unresponsive/beachballing app blocks for the system default (~6s). Phantom nil-role windows are exactly the windows owned by 
SYMPTOM: All hotkeys and border updates freeze for seconds at a time whenever any app stops responding (app launch, heavy load, beachball), recovering when the app does.
FIX: Call AXUIElementSetMessagingTimeout (e.g. 0.5s) on app elements, and/or perform AX property queries off-actor (like the CGWindowList snapshot already is) and apply results in a second actor hop.

### [low] GridFocus.focusLoopDetector mutated from concurrent contexts (array data race)
LOC: grid-server/Sources/GridServer/Grid/GridFocus.swift:432
MECH: GridFocus is a plain class holding 'private var focusLoopDetector = FocusLoopDetector()' (GridFocus.swift:63), a struct whose mutating observe() does recent.removeAll + recent.append (FocusLoopDetector.swift:56-62). focusWindowByID calls focusLoopDetector.observe at GridFocus.swift:432 and is invoked concurrently from: (a) the command path — each socket request Task via GridCommandRouter -> moveFocus/cycleFocus (GridFocus.swift:242, 261, 359); (b) GridReconciler.handlePendingLaunchWindow on the 
SYMPTOM: Focus mismatch retry storms that the loop detector should have stopped (unbounded focus ping-pong between two windows), or a rare crash inside FocusLoopDetector.observe during simultaneous picker-launch and hotkey focus.
FIX: Isolate GridFocus (or at least the detector) to an actor; it is only ever used via await already.

### [medium] focusWindow can never fail: success Bool hardcoded true, all CG/SLPS errors discarded, state focus set unconditionally
LOC: grid-server/Sources/GridServer/WindowManipulator.swift:481
MECH: focusWindowWithRaise (WindowManipulator.swift:458-482) discards the CGError of SLPSSetFrontProcessWithOptions ('_ =' at :468), both SLPSPostEventRecordTo results inside makeKeyWindow (:446, :452), and AXUIElementPerformAction(kAXRaiseAction) (:478), then 'return true' unconditionally (:481). Its only failure branch (GetProcessForPID fails, :461-464) routes to focusWindowFallback (:485-497) which also discards mssClient results and returns true unconditionally (:496). Therefore focusWindow(pid:wi
SYMPTOM: Focus command appears to succeed (CLI gets success, border moves, state.focusedWindowID updates) but keyboard focus never changed; subsequent direction-based focus navigation starts from the wrong window; err.focus/warn.focus never appear in logs eve
FIX: Propagate the CGError/AXError results: treat SLPSSetFrontProcessWithOptions != .success and AXRaise failure (or missing AX element) as failure, return false, and only call setFocusedWindow on genuine success.

### [low] MSSClient availability cache is permanent: resetAvailabilityCache() has zero callers
LOC: grid-server/Sources/GridServer/MSSClient.swift:150
MECH: MSSClient.isAvailable (MSSClient.swift:106-147) probes mss_handshake up to 3 times on first call and then caches the verdict forever (cachedAvailable :99, set at :124/:143). The escape hatch resetAvailabilityCache() (:150-154) is documented as 'call on system wake to re-probe MSS' but a repo-wide grep finds NO callers — StateManager.handleSystemWoke (StateManager.swift:2058-2078) refreshes state and rebuilds AX observers but never touches MSS. Consequences for the long-lived MSSClient instances 
SYMPTOM: Window-to-space moves silently use the unreliable SkyLight fallback (or fail outright after a Dock restart) for the rest of the session; recovery requires restarting grid-server even though MSS is healthy again.
FIX: Call resetAvailabilityCache() from the wake handler (and on repeated mss.fail of real operations trigger a re-probe/reconnect) instead of leaving the cache permanent.

### [low] BFD config watcher silently dies after first atomic-save (rename) and never re-arms
LOC: grid-server/Sources/GridServer/BFD/BFDManager.swift:162
MECH: BFDManager.startConfigWatcher (BFDManager.swift:162-194) opens the config file fd exactly once at startup ('let fd = open(expandedPath, O_EVTONLY)', :164) and installs a DispatchSource with eventMask [.write, .delete, .rename, .extend]. The event handler (:173-187) only debounces and calls reload(); it never cancels and recreates the watcher. When an editor saves via atomic rename (vim, VS Code safe write), the watched inode is renamed/deleted: the source fires once (one final reload), but the f
SYMPTOM: User edits ~/.config/thegrid/bfd.yaml; the first save after server start applies, but subsequent saves silently do nothing — new/changed hotkeys don't take effect until the server is restarted, with no error anywhere.
FIX: Re-arm the watcher (cancel + reopen fd) inside the event handler on .rename/.delete, or call startConfigWatcher() from reload() like GridConfig does; log when open() fails.

### [low] BFDExecutor pipe-buffer deadlock: waitUntilExit before reading stdout/stderr
LOC: grid-server/Sources/GridServer/BFD/BFDExecutor.swift:46
MECH: BFDExecutor.execute (BFDExecutor.swift:30-64) wires stdout/stderr to Pipes (:25-28), then calls 'process.waitUntilExit()' at :46 BEFORE reading the pipes at :49-50. If the hotkey command writes more than the ~64KB pipe buffer, the child blocks on write while the parent blocks in waitUntilExit — a classic deadlock. The 30s timeout work item (:34-44) then process.terminate()s the command mid-run and logs 'Command timed out after 30s', so a valid verbose command is killed and reported as a timeout.
SYMPTOM: A hotkey bound to a command with verbose output appears to hang, then is killed after 30 seconds with a 'timed out' bfd.error; the command's effect is partial (terminated mid-run) even though the command itself was fine.
FIX: Read both pipes (readabilityHandler or readDataToEndOfFile on a background thread) before/concurrently with waitUntilExit, mirroring EnrichmentTypes.runProcess.

### [low] Terminal show() proceeds after AX failure, leaving a focused, opacity-1 window stranded off-screen (and can persist the off-screen frame)
LOC: grid-server/Sources/GridServer/Grid/GridTerminalManager.swift:220
MECH: hide() parks the Ghostty window at (-10000,-10000) with opacity 0 (GridTerminalManager.swift:181-186). show() is supposed to restore the frame first: :220-222 'if let element = windowManipulator.getAXElement(pid: pid, windowID: wid) { _ = windowManipulator.setWindowFrame(element: element, frame: targetFrame) }'. Both failure modes are ignored: getAXElement returning nil skips the reposition entirely; a false return from setWindowFrame is discarded via '_ ='. Either way, show() continues uncondit
SYMPTOM: Terminal hotkey 'shows' the terminal but nothing appears; keystrokes go to an invisible window; in the persistence-poisoning variant the terminal never appears again on that display even across restarts, until terminal-frames.json is deleted.
FIX: In show(), treat getAXElement-nil / setWindowFrame-false as a hard failure: retry or abort without flipping isHidden/opacity/focus; in hide(), refuse to save a frame whose origin is at/near the off-screen sentinel.

### [low] AX observer registration failure (-25204) at app launch is never retried — app runs unobserved
LOC: grid-server/Sources/GridServer/StateManager.swift:1202
MECH: handleApplicationLaunched (StateManager.swift:1934) calls createObserver immediately on the NSWorkspace launch notification (:1945). createObserver fires 'Task { @MainActor in if observer.observe(stateManager: self) { await self.addApplicationObserver(...) } }' (:1202-1206) — when observe() returns false, the result is silently discarded; no retry timer, no backoff. observe() returns false when AXObserverAddNotification fails for all 8 notifications (ApplicationObserver.swift:85-93 'guard succes
SYMPTOM: For the affected app: new windows take up to 3s to tile (poll latency), window destroys leave stale cell entries until the next poll/validator pass, and per-window move/resize tracking is absent — intermittent, app-specific sluggish/incorrect tiling 
FIX: On observe() failure, schedule bounded retries with delay (e.g. 0.5s/1s/2s) before giving up, and include pid/app in the failure log.

### [low] not_standard creation bail is terminal — a window misclassified as floating at creation never gets re-evaluated
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:603
MECH: handleWindowCreated classifies the window after the isTileable gate (GridReconciler.swift:602-611). classifyWindow returns .floating for an AXStandardWindow with no fullscreen button (GridAssignment.swift:94); hasFullscreenButton comes from a one-shot AX query at creation (StateManager.swift:1483-1491) — apps that populate window buttons slightly after creating the AX element read as button-less. Unlike the not_tileable path (:589 rejectWindow), the not_standard bail does NOT reject the window, 
SYMPTOM: A slow-starting app's main window occasionally floats untiled despite being a normal standard window; the same window DOES get tiled if the user reapplies the layout, confirming the classification disagreement between the create path and the apply pa
FIX: Track not_standard bails for a short grace period and re-classify on the next move/resize or sweep tick; also reconcile classifyWindow (create path) with the apply path so both agree.

### [low] Fire-and-forget dead-window prune races setFocus, recording a dead/wrong lastFocusedWid
LOC: grid-server/Sources/GridServer/Grid/GridFocus.swift:230
MECH: cycleFocus (GridFocus.swift:228-234) and focusCellByID (:318-323) prune dead windows from a LOCAL copy of the cell list but remove them from GridState via detached 'Task { await gridState.removeWindow(...) }' (:230 and :320) with no ordering relative to the subsequent 'await gridState.setFocus(spaceID:cellID:windowIndex:)' (:243/:263/:362). The windowIndex is computed against the filtered local array, but if setFocus reaches the GridState actor before the prune Task, GridState.setFocus (GridStat
SYMPTOM: Right after a window in a cell closes, focusing or cycling into that cell raises a different window than the one last used.
FIX: Await the removeWindow calls (they are already in an async context) instead of detaching them, so setFocus indexes the pruned state.

### [low] FocusLoopDetector blind spots: nil-actual bypasses verification entirely, 3-way loops never trip, and attempt-1 raise always fires
LOC: grid-server/Sources/GridServer/Grid/GridFocus.swift:421
MECH: The detector is consulted only inside the mismatch branch 'if let actualWID = actualFocusedWID, actualWID != windowID' (GridFocus.swift:421). (1) When metadata.focusedWindowID is nil (cleared by handleWindowDestroyed StateManager.swift:1531-1532 or handleApplicationTerminated :1958), verification passes vacuously and no mismatch/loop tracking happens at all. (2) FocusLoopDetector keys on the unordered (requested,actual) pair (FocusLoopDetector.swift:17-25, threshold 3 in 2s at :34-35), so a rota
SYMPTOM: Under a 3-window focus rotation or after the focused window is destroyed mid-command, focus retries/raises continue without loop detection, producing visible raise flicker.
FIX: Track loops per requested-window (not pair), include the nil-actual case in observation, and add a short suppression window after a trip.

### [low] DispatchQueue.main.sync from inside the StateManager actor (removeObserver)
LOC: grid-server/Sources/GridServer/StateManager.swift:1222
MECH: removeObserver (StateManager.swift:1217-1230) executes 'DispatchQueue.main.sync { observer.stopObserving() }' while running on the StateManager actor's cooperative-pool thread. It is called from handleApplicationTerminated (:1965, on every app quit) and shutdown (:175). Blocking a cooperative-pool thread on the main queue violates Swift concurrency's forward-progress contract: the actor is stalled for as long as the main thread is busy (border pool rebuilds, picker rendering, AX callbacks), and 
SYMPTOM: Brief whole-server stalls when apps quit while the main thread is busy; latent deadlock if any future main-thread code blocks on the actor.
FIX: Make removeObserver async and use await MainActor.run { observer.stopObserving() } like rebuildAXObservers does.

### [low] GridState.load() races startup event handling and clobbers in-memory mutations
LOC: grid-server/Sources/GridServer/main.swift:142
MECH: main.swift:140-143 creates GridState and spawns an unordered 'Task { await gridState.load() }' while a sibling Task (main.swift:160+) starts StateManager and wires GridReconciler. Nothing sequences load() before the reconciler registers with EventRouter (GridReconciler.swift:304-306) or before commands can arrive. GridState.load (GridState.swift:113-124) wholesale replaces 'spaces = decoded.spaces' — any assignment/migration GridState performed between server start and load completion (e.g. a wi
SYMPTOM: On server restart, a window tiled in the first instants disappears from its cell once persisted state loads; occasional 'state reverted' impressions right after restart.
FIX: await gridState.load() before wiring GridReconciler/StateManager (it is cheap), or have load() merge instead of replace when in-memory state is non-empty.

### [low] SocketServer.sendMessage called concurrently per socket — JSONL frames can interleave
LOC: grid-server/Sources/GridServer/SocketServer.swift:233
MECH: sendMessage (SocketServer.swift:233-254) writes 'data + \n' with a single send() call but is invoked from multiple concurrent contexts for the same client socket with no per-socket serialization: (a) request responses arrive via MessageHandler completion inside per-request Tasks (SocketServer.swift:227-229, MessageHandler.swift:52); (b) EventBroadcaster.broadcast iterates ALL clientSockets (EventBroadcaster.swift:32-39) and calls server.sendMessage from whatever context fired the event. A client
SYMPTOM: CLI/viewer occasionally receives unparseable JSON or a response glued to an event line, surfacing as client-side parse errors or timed-out commands under event-heavy load.
FIX: Funnel all writes for a given client socket through a per-socket serial queue (or the existing socketQueue).

### [low] BFD blacklist and per-app overrides match localizedName while docs specify bundle IDs — silently never match
LOC: grid-server/Sources/GridServer/BFD/BFDKeyHandler.swift:244
MECH: BFDKeyHandler.handleEvent resolves the frontmost app as NSWorkspace.shared.frontmostApplication?.localizedName (BFDKeyHandler.swift:244, e.g. 'Finder') and checks blacklist.contains(appName) (:247) and appHotkeys[appName] (:255) against that display name. The project documentation (CLAUDE.md 'BFD Hotkey Configuration') documents both keys as bundle identifiers: 'blacklist: - com.apple.finder' and 'apps: com.example.app:'. A user who follows the docs gets entries that can never match any localize
SYMPTOM: Hotkeys keep firing inside apps the user blacklisted by bundle ID, and app-specific overrides configured per the documented format are silently ignored.
FIX: Match against bundleIdentifier (or both bundleIdentifier and localizedName) in handleEvent, or fix the docs and warn at config load when a blacklist/apps key looks like a bundle ID but matching is by name.

### [low] assignAutoFlow silently drops every window when all cells are locked
LOC: grid-server/Sources/GridServer/Grid/GridAssignment.swift:372
MECH: GridAssignment.assignAutoFlow filters locked cells out of the candidate list (GridAssignment.swift:370 'sortedCells = sortedCells.filter { !lockedCells.contains($0) }') and then bails with 'guard !sortedCells.isEmpty else { return }' (:372) — no log, no error. Any window in the batch that did not match a locked app rule simply never appears in result.assignments, so applyLayout (GridApply.swift:266 setWindowAssignments) commits an assignment map that omits those windows entirely: they are untrac
SYMPTOM: After applying a layout where all cells are locked to specific apps, windows of other apps are left floating wherever they were — no placement, no border, no log explaining why.
FIX: Log a warn (e.g. assign.dropped with wids) when the filtered cell list is empty, and/or fall back to assigning to the least-populated locked cell rather than dropping windows.

### [high] Nudge double-enter leaks a live CGEventTap and permanently suppresses the reconciler
LOC: grid-server/Sources/GridServer/Grid/GridCommandRouter.swift:779
MECH: Each '@nudge enter' request runs in its own unstructured Task (BFDManager.swift:137 for hotkeys, MessageHandler.swift:52 for socket requests) against the plain non-actor GridCommandRouter. handleNudge's idempotency check `if nudgeKeyHandler != nil` (GridCommandRouter.swift:779) is followed by a suspension point `await resolveActiveSpaceID()` (GridCommandRouter.swift:785) before the handler is finally stored at GridCommandRouter.swift:830. A second concurrent enter (double-tap of the hotkey befor
SYMPTOM: After double-tapping the nudge hotkey and later exiting nudge mode: hjkl/arrow keys stop reaching apps and instead keep moving/resizing the focused window forever (until server restart); window manager stops tiling new windows and never removes close
FIX: Make nudge session state actor-isolated (or route handleNudge through a serial queue); give ActionToken a unique id and make endAction consume it exactly once; stop-and-replace any existing handler before installing a new one.

### [medium] Command ingress is fully unserialized: per-request/per-keypress unstructured Tasks run grid commands concurrently — log-confirmed 4-way overlap and lost resize increments
LOC: grid-server/Sources/GridServer/MessageHandler.swift:52
MECH: Every socket request spawns a bare `Task { ... handler(request) }` in MessageHandler.handle (MessageHandler.swift:52-84), and every BFD '@' hotkey — including auto-repeats at the 50ms rate limit — spawns its own `Task { await router.dispatch(command) }` in BFDManager.handleInternalCommand (BFDManager.swift:137). GridCommandRouter.dispatch (GridCommandRouter.swift:158) is an async method on a plain class with no queue, so N in-flight commands execute genuinely concurrently and complete in arbitra
SYMPTOM: Held-key resize grows/shrinks less than the number of keypresses and windows visibly stutter to wrong intermediate sizes; rapid focus-hotkey repeats land on the wrong window or ping-pong; interleaved move/apply commands place windows from a half-appl
FIX: Funnel GridCommandRouter.dispatch through a single serial executor (make the router an actor, or an AsyncStream command queue consumed by one task), coalescing repeat-key commands.

### [medium] All @notify actions collapse to launch-or-toggle: handleNotify ignores action and args, and buildCommand drops notify payload params
LOC: grid-server/Sources/GridServer/Grid/GridCommandRouter.swift:880
MECH: MessageHandler registers ten notify RPCs (grid.notify.show/hide/toggle/push/list/dismiss/clear/count/assign/unassign, MessageHandler.swift:1907-1962) that build command strings like '@notify push ...' and dispatch through the router. dispatch routes the whole 'notify' domain to handleNotify (GridCommandRouter.swift:197-198), but handleNotify (GridCommandRouter.swift:880-930) never reads cmd.action or cmd.args: it only checks whether GridNotify.app is running, launches it if not (:885-919), or po
SYMPTOM: Any client calling notify RPCs gets wrong-direction behavior: hide shows the panel, push/dismiss/clear toggle visibility instead of acting, and notification payloads vanish — all while the RPC returns ok:true ('toggled').
FIX: Switch handleNotify on cmd.action (separate show/hide/toggle distributed notifications and forward push/list/dismiss payloads), and teach buildCommand to pass notify-specific params or build notify commands explicitly.

### [low] MessageHandler.handlers dictionary is written after the socket starts accepting — unsynchronized Dictionary race and a startup window where all grid.* methods 404
LOC: grid-server/Sources/GridServer/MessageHandler.swift:26
MECH: MessageHandler is a plain class with `private var handlers: [String: RequestHandler]` (MessageHandler.swift:15). register() mutates it with no lock (MessageHandler.swift:26). main.swift starts the socket server at main.swift:122 (`try socketServer.start()`), but registerGridHandlers — ~40 more register() calls on the main thread — only runs at main.swift:229. SocketServer accepts connections on a concurrent clientQueue (SocketServer.swift:10, :133) and handleRequest calls handler.handle (SocketS
SYMPTOM: Rare crash at startup, or first hotkey/CLI command after a server restart fails with 'Method not found: grid.focus' despite the server being up.
FIX: Register all handlers (or at least construct MessageHandler fully) before socketServer.start(), or guard the handlers map with a lock/actor.

### [low] Nudge keystrokes spawn unordered Tasks doing read-modify-write on the cached frame — held arrow key loses steps
LOC: grid-server/Sources/GridServer/Grid/GridCommandRouter.swift:803
MECH: NudgeKeyHandler.onNudge fires synchronously on the main thread per keypress, and the router callback bridges each one into an independent unstructured `Task { ... gridNudge.move(...) }` (GridCommandRouter.swift:803-813) with no ordering or coalescing. GridNudge.move (GridNudge.swift:67-104) is a read-modify-write against cached state: ManipulationContext.from reads the frame from StateManager's cached snapshot (WindowManipulator.swift:29-38, `frame: windowState.frame`), computes newOrigin = curr
SYMPTOM: Holding an arrow/hjkl key in nudge mode moves the window far fewer steps than keypresses (or it jumps backward), with border overlay desyncing from the window.
FIX: Serialize nudge actions through one queue/AsyncStream per session and carry the frame forward between steps (or re-read the live AX frame) instead of the cached StateManager frame.

### [low] Router-level stale-space TOCTOU: spaceID resolved once, then acted on across awaits — '@state reset' and resize/layout can hit the space the user just left
LOC: grid-server/Sources/GridServer/Grid/GridCommandRouter.swift:305
MECH: resolveActiveSpaceID (GridCommandRouter.swift:305-307) snapshots wmState and derives the active space, then every handler proceeds through further awaits using that string. With commands queuing behind concurrent dispatch Tasks (finding 2: up to 4 in-flight observed) plus the reconciler's awaitWakeCompletion gate at GridCommandRouter.swift:167, the gap between hotkey press and spaceID resolution-vs-action can span a user space switch. Concrete paths: handleState 'reset' (GridCommandRouter.swift:
SYMPTOM: Right after switching spaces, a queued reset/resize/layout hotkey silently mutates the previous space: its grid state is wiped or its windows get re-laid-out and later bounced by the sweep.
FIX: Re-resolve (or verify) the active space immediately before the mutating call inside each handler, aborting with a 'space changed' error on mismatch.

### [medium] Wake unconditionally resumes StateValidator while screen is still locked, ax_orphan-pruning real windows
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:962
MECH: Lid close / lock fires screenLocked and systemWillSleep, both pausing the validator (GridReconciler.swift:456-460 -> StateValidator.pause() :97). On lid open, NSWorkspace.didWakeNotification routes .systemWoke; GridReconciler.handleSystemWake Step 0 'unconditionally resume the validator' (GridReconciler.swift:959-962) runs BEFORE the user has unlocked (com.apple.screenIsUnlocked has not fired). Step 1.5 resets all orphan counts (:985), then Step 2 runs validate() immediately (:990) and the 30s p
SYMPTOM: After waking the laptop and pausing at the login screen, previously tiled windows are untiled / missing from their cells; borders blank for those windows.
FIX: On wake, only resume the validator if the screen is not locked (track lock state from the distributed notifications), or defer resume until screenUnlocked when a lock preceded the sleep.

### [medium] No zero-bounds guard in layout apply: wake/reconnect refresh can tile every window against CGRect.zero
LOC: grid-server/Sources/GridServer/Grid/GridApply.swift:136
MECH: applyLayoutBody gets display bounds via gridFocus.getDisplayBoundsForSpace (GridApply.swift:136), which resolves to 'display.visibleFrame ?? display.frame ?? .zero' (GridFocus.swift:941) and returns .zero when the space or display is unknown (GridFocus.swift:944,953). The result is fed straight into GridLayout.calculateLayoutWithRatios as screenRect (GridApply.swift:148-150) with no .zero/empty check, and placements are applied via AX (GridApply.swift:383, :476). A nil frame is a real state: Dis
SYMPTOM: After sleep/wake or replugging a dock, all tiled windows pile up tiny in the top-left corner of a display and stay there.
FIX: Guard applyLayoutBody (and refreshAllDisplays per-display) against zero/degenerate displayBounds: log and skip/retry instead of computing placements from CGRect.zero.

### [medium] Resolution/scaling/dock changes never reapply layouts: displayReconfigured is UUID-diff-only and ignored by the reconciler
LOC: grid-server/Sources/GridServer/StateManager.swift:1903
MECH: NSApplication.didChangeScreenParametersNotification fires for resolution changes, scaling changes, display rearrangement, and Dock size/visibility changes. WorkspaceObserver routes it as .displayReconfigured(displayUUID: "") (WorkspaceObserver.swift:144-149). StateManager.handleDisplayConfigurationChanged (StateManager.swift:1888) refreshes displays/spaces (so state.displays gets the NEW frames/visibleFrames) but only diffs display UUID SETS (:1903-1905) to decide what to route — a geometry-only
SYMPTOM: After changing resolution/scaling, moving the Dock, or a display finishing late negotiation, tiled windows are mis-sized/mis-positioned indefinitely (off-screen or overlapping the Dock) until a manual layout command.
FIX: In handleDisplayConfigurationChanged, diff per-display frame/visibleFrame in addition to UUIDs and route a geometry-changed event; have GridReconciler handle it with refreshAllDisplays (debounced).

### [high] Wake space-ID migration pairs a stale persisted snapshot positionally (string-sorted) and clobbers destination space state
LOC: grid-server/Sources/GridServer/Grid/GridState.swift:236
MECH: handleSystemWake Step 1 builds per-display space lists sorted as STRINGS ('spaceIDs.sorted()' on [String], GridReconciler.swift:974 — so "999" > "1001" lexicographically) and calls gridState.migrateSpaceIDs (GridReconciler.swift:977). migrateSpaceIDs (GridState.swift:236-277) pairs oldSpaceList[i] with newSpaceList[i] positionally (:247-249) where oldSpaceList comes from GridState.displaySpaces (:244). That snapshot has exactly ONE writer: migrateSpaceIDs itself (:270) — confirmed via grep (writ
SYMPTOM: After sleep/wake, spaces swap or lose their layouts and cell assignments: windows tile into another space's layout, or a space's grid config disappears entirely ~30s after wake.
FIX: Keep displaySpaces updated on every refreshSpaces/migrateSpace (snapshot at sleep time, not last wake), sort numerically, and refuse to overwrite a destination space that already hasSignificantState.

### [low] Reconciler's display-disconnect zombie prune is dead code: spaces are refreshed out of wmState before the event is routed
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:1118
MECH: StateManager.handleDisplayConfigurationChanged calls refreshDisplays()/refreshSpaces() FIRST (StateManager.swift:1899-1900; refreshSpaces fully replaces state.spaces from SLSCopyManagedDisplaySpaces at :493), and only THEN routes .displayDisconnected (:1922-1927). EventRouter dispatches handlers sequentially (EventRouter.swift:202-214) in registration order — StateManager first (registered in start(), StateManager.swift:120, main.swift:161) so its handleDisplayDisconnected (:1712-1724) refreshes
SYMPTOM: On display unplug, the intended GridState cleanup never happens; windows macOS moved to the surviving display remain untiled with stale cell assignments pinned to the migrated space.
FIX: Capture the disconnected display's space IDs (or pass them in the event payload) BEFORE refreshSpaces rewrites state.spaces, or have the reconciler diff GridState's own per-display space map instead of post-refresh wmState.

### [high] Atomic setCellAssignments path silently drops assignment deltas (no rebuild when cell+focus unchanged)
LOC: grid-server/Sources/GridServer/Borders/SimpleBorderManager.swift:189
MECH: GridReconciler.syncBordersForSpace ALWAYS passes focusedWindowID (GridReconciler.swift:1249-1252), so every reconciler border push takes the 'atomic' branch of setCellAssignmentsImpl (SimpleBorderManager.swift:165). That branch only dispatches on three conditions: display/cell changed -> rebuildBorderPool (:178-179), focused window changed -> reassignBorders (:180-184), else -> refreshBorderPositions() (:185-189). The assignments-content comparison `let assignmentsChanged = oldAssignments != ass
SYMPTOM: Orphaned gray border following a window that was moved to another cell; missing border on a window newly added to the active split cell; stack-indicator tick count wrong (shows N windows when N±1 exist) after sending a window to/from the focused cell
FIX: In the atomic branch's final else, compare oldAssignments (filtered to the active cell) against the new assignments and call rebuildBorderPool (or targeted acquire/release + applyActiveStyle) when the cell membership changed; otherwise keep refreshBo

### [medium] @unchecked Sendable violated: bare Task{} logging blocks read freePool/activeBorder/focusedWindowID off the main queue
LOC: grid-server/Sources/GridServer/Borders/SimpleBorderManager.swift:676
MECH: The class is declared `@unchecked Sendable` with the invariant 'all mutable state is accessed on DispatchQueue.main' (SimpleBorderManager.swift:26-28). But several methods spawn bare `Task { }` blocks whose closures evaluate the log-data dictionary INSIDE the task. These methods are plain (non-@MainActor) class methods, so `Task {}` inherits no actor isolation and runs on the global concurrent executor — off the main queue. rebuildBorderPool's Task (:676-685) reads `activeBorder`, `focusedWindow
SYMPTOM: Rare unexplained server crash during heavy border churn (cell/display switching); incorrect poolSize/focused/tabbed values in bdr.rebuild / bdr.pool.releaseAll log events poisoning post-mortems.
FIX: Capture all mutable-state values into immutable locals BEFORE spawning the Task (as destroyAllBorders :555-563 already does correctly), or make the class @MainActor and drop @unchecked Sendable.

### [low] BorderWindow.retarget commits targetWindowID before the bounds guard; failure leaves the active border visibly stuck on the old window
LOC: grid-server/Sources/GridServer/Borders/BorderWindow.swift:449
MECH: retarget(to:) (BorderWindow.swift:442) assigns `targetWindowID = newTargetID` at :449 BEFORE the SLSGetWindowBounds guard at :453. If bounds for the new target can't be read (window already destroyed — realistic given the known reconciler bugs where destroy events are dropped during suppression, or a focus change racing window close), it logs err.bdr.retarget and returns at :463 WITHOUT repositioning, without moveToTargetSpace, and without hiding. The border stays fully visible at the OLD window
SYMPTOM: A red/active border rectangle frozen at the previous window's position (possibly over empty desktop after that window closes) while focus has moved elsewhere; clears only on the next cell switch.
FIX: Make retarget return Bool (or only commit targetWindowID after the bounds guard) and have reassignBorders hide/release the border and acquire a fresh one (or fall back to rebuildBorderPool) on failure.

### [low] Resize redraw failure leaves border invisible (alpha 0) while isVisible stays true — position-only updates never restore it
LOC: grid-server/Sources/GridServer/Borders/BorderWindow.swift:404
MECH: performUpdate (BorderWindow.swift:325) on a resize hides the window via `SLSSetWindowAlpha(connectionID, windowID, 0.0)` at :368 when wasVisible, but does NOT set isVisible=false. Visibility is restored only inside the conditional at :400-417: requires wasVisible AND currentStyle != nil AND `SLWindowContextCreate` at :404 succeeding. If context creation fails, the code's own comment at :416 ('Note: If context creation fails, border stays hidden until next update') is wrong about recovery: isVisi
SYMPTOM: Focused window's border silently disappears after a window resize and stays gone while moving the window around; reappears only after switching focus/cell or changing config.
FIX: On the resize-hidden path, either set isVisible=false when redraw cannot complete (so the next updateStyle/show repairs it) or retry show() with alpha restore in the failure branch; also log the SLWindowContextCreate failure at :404 (currently silent

### [low] queryBorderInfo does DispatchQueue.main.sync from the socket client thread — borders.query RPC hangs if main is wedged
LOC: grid-server/Sources/GridServer/Borders/SimpleBorderManager.swift:486
MECH: queryBorderInfo (SimpleBorderManager.swift:479-492) is the only synchronous-blocking entry point in the file: when not on main it does `DispatchQueue.main.sync { ... }` at :486-488. Its sole caller is the borders.query RPC handler (MessageHandler.swift:1323), and MessageHandler.handle runs synchronously on SocketServer's clientQueue (SocketServer.swift:133-134 `clientQueue.async { self?.handleClient(...) }`, :189 `handleRequest(request, ...)` inside the per-client read loop). So the client's ent
SYMPTOM: `thegrid` borders.query CLI invocation hangs forever (no response, no error) when the server's main thread is busy/wedged; one leaked client-handler thread per attempt.
FIX: Convert queryBorderInfo to the same async withCheckedContinuation + main.async bridge used by the BorderRendering conformance (:1092-1165) and make the RPC handler await it inside its Task.

### [low] handleWindowDestroyed prunes cellAssignmentsPerDisplay but not windowOrderPerDisplay — stack indicator counts dead windows
LOC: grid-server/Sources/GridServer/Borders/SimpleBorderManager.swift:372
MECH: handleWindowDestroyedImpl (SimpleBorderManager.swift:346-376) removes the dead wid from every display's cellAssignmentsPerDisplay (:373-375, with an explicit 'prevent unbounded growth' comment) but never touches windowOrderPerDisplay, cellStackModesPerDisplay, or displayFramePerDisplay. computeStackIndicator (:924-952) prefers the cached order verbatim: `if let providedOrder = windowOrderPerDisplay[displayUUID]?[cellID] { windowsInCell = providedOrder }` (:934-935), so totalCount includes the de
SYMPTOM: Tabbed-cell stack indicator briefly (or for several seconds under reconciler suppression) shows the wrong tab count / wrong position after closing a window in the active cell.
FIX: In handleWindowDestroyedImpl, also strip the wid from every windowOrderPerDisplay[*][*] array (mirroring the cellAssignments cleanup loop).

### [medium] applyLayoutBody writes layout/assignments back to a space ID that spaceIDReassigned migrated away mid-apply, resurrecting a zombie space
LOC: grid-server/Sources/GridServer/Grid/GridApply.swift:266
MECH: applyLayoutBody captures spaceID as a plain String parameter (GridApply.swift:120) and performs ~20 awaits: reads wmState at :135, ratios at :143-144, previousAssignments at :167, computes placements, applies AX at :249. Suppression via executeAction (GridApply.swift:94 -> GridReconciler.swift:99) only sets suppressionDepth; it does NOT block spaceIDReassigned, which is explicitly processed BEFORE the suppression guard (GridReconciler.swift:430-439: 'Space ID reassignment must always be processe
SYMPTOM: Right after a space transition (fullscreen enter/exit, space switch — logs show 130 live migrations incl. bounce pairs 3→384→3), a layout apply visibly positions windows but the grid then behaves as if the apply never happened: focus/send/swap comman
FIX: Re-resolve the space ID (or detect migration via a GridState generation/alias map) immediately before the state-write phase at :260-266, or have executeAction defer spaceIDReassigned processing and replay it after the action completes.

### [low] Picker-launched window claimed during an in-flight applyLayout is silently clobbered by setWindowAssignments
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:800
MECH: handle() checks pendingLaunchTarget for windowCreated BEFORE the suppression guard (GridReconciler.swift:421-428: 'Picker-launched windows must be claimed even during move suppression'), so handlePendingLaunchWindow runs while a layout.apply executeAction is suppressing. At GridReconciler.swift:800-804 it does gridState.prependWindow(windowID, toCellID: target.cellID), setFocus, and applyCellLayout. If an applyLayoutBody for the same space is between its assignment read (GridApply.swift:167 getW
SYMPTOM: App launched via the picker lands in the chosen cell, then on the next layout reapply/focus operation it is treated as unassigned: it gets re-tiled into the wrong cell, loses its slot, or is ignored by focus navigation, especially when the launch coi
FIX: Make applyLayoutBody re-read assignments and merge (rather than wholesale replace) at write time, or queue pendingLaunch claims while suppressionDepth > 0 and apply them after the action ends.

### [high] No per-space serialization of applyLayout: concurrent request Tasks interleave cycleLayout state wipes and wholesale assignment writes, producing mixed-layout state
LOC: grid-server/Sources/GridServer/Grid/GridApply.swift:120
MECH: Every socket request runs in its own unstructured Task (MessageHandler.swift:52 'Task {' inside handle()), clients are handled on a concurrent queue (SocketServer.swift:10 'attributes: .concurrent', :133 clientQueue.async), and GridCommandRouter.dispatch (GridCommandRouter.swift:158-210) has no lock; executeAction (GridReconciler.swift:91-129) only increments suppressionDepth — it provides zero mutual exclusion between two concurrent bodies. So two rapid 'layout cycle' hotkey presses run concurr
SYMPTOM: Hammering the layout-cycle/apply hotkey (or a layout command landing during a display-reconnect refresh) leaves currentLayoutId and the cell map from different layouts: subsequent cell/resize commands error with cellNotFoundInLayout or operate on pha
FIX: Serialize the apply pipeline per space (actor or AsyncSemaphore keyed by spaceID) around the whole read-compute-write body, and move the cycleLayout/previousLayout state mutation inside that critical section.

### [high] resize grow/shrink is inverted when the focused window is the last in its cell stack
LOC: grid-server/Sources/GridServer/Grid/GridResize.swift:93
MECH: adjustFocusedSplit picks the boundary as boundaryIndex = focused index, but when the focused window is the last in the stack it falls back to the previous boundary (GridResize.swift:93-96: 'var boundaryIndex = idx; if boundaryIndex >= ratios.count - 1 { boundaryIndex = ratios.count - 2 }') WITHOUT negating delta. adjustSplitRatio (GridLayout.swift:687-718) does newFirst = ratios[index] + delta; newSecond = ratios[index+1] - delta. For focused idx < last, ratios[idx] grows — correct. For focused 
SYMPTOM: With the bottom/right-most (or any last-in-stack, e.g. newly opened) window focused, the grow hotkey makes it smaller and shrink makes it bigger — inverted resize behavior. resize.split.done logged 63 times, so this path is heavily used.
FIX: When clamping boundaryIndex to ratios.count - 2 for the last window, negate delta (or call adjustSplitRatio with index = idx-1 and delta = -delta).

### [low] layout cycle/previous destroys all cell assignments before applyLayout runs — a thrown apply leaves the space wiped with no recovery
LOC: grid-server/Sources/GridServer/Grid/GridCommandRouter.swift:399
MECH: handleLayout 'cycle' calls gridState.cycleLayout (GridCommandRouter.swift:399) which immediately commits setCurrentLayout (GridState.swift:285-296), wiping space.cells, focusedCell, focusedWindow and track ratios, BEFORE applyLayout is invoked at :400 (same for 'previous' at :409-410). If applyLayout then throws — gridConfig.getLayout at GridApply.swift:132 (config reloaded/renamed between getLayoutIDs and getLayout), the guard-let at GridApply.swift:81-86, or any future throw in the body — the 
SYMPTOM: A failed layout cycle (e.g. config edited concurrently) leaves every window on the space untiled and focus state empty; user must manually reapply a layout. Transient empty-cell window also lets a sweep mis-handle windows as unassigned.
FIX: Move the cycleLayout/setCurrentLayout mutation inside applyLayoutBody (it already calls setCurrentLayout at GridApply.swift:263) and pass only the computed target layout ID from the router; never pre-commit the wipe.

### [medium] assignWindow/removeWindow equalize splitRatios on every membership change while setWindowAssignments preserves them — cell send wipes custom splits in both cells
LOC: grid-server/Sources/GridServer/Grid/GridState.swift:339
MECH: GridCellOps.sendWindow moves a window via gridState.removeWindow + gridState.assignWindow (GridCellOps.swift:118-119). removeWindow resets the source cell's ratios to equal (GridState.swift:415-419: 'cell.splitRatios = equalRatios(cell.windows.count)') and assignWindow resets the target cell's ratios to equal (GridState.swift:339). The reapplyLayout that follows (GridCellOps.swift:127) reads those now-equalized ratios at GridApply.swift:205 and places windows accordingly. This contradicts the pr
SYMPTOM: Carefully adjusted split ratios in a cell snap back to equal whenever any window is sent into or out of that cell, or when a new window opens and is auto-assigned to it — while a plain layout reapply preserves them, making the behavior feel random.
FIX: In assignWindow/removeWindow, use recalculateSplitsAfterAddition/recalculateSplitsAfterRemoval (already implemented in GridLayout.swift:781-825 and currently dead for this path) instead of equalRatios.

### [medium/SUSPECTED] deriveSpaceFromDisplay rewrites window.spaces to the display's CURRENT space, overriding SLS truth for windows on inactive spaces
LOC: grid-server/Sources/GridServer/StateManager.swift:952
MECH: deriveSpaceFromDisplay (StateManager.swift:945-982) sets 'window.spaces = [display.currentSpaceID]' for ANY non-sticky window whose geometric center lands on a display (:952-955), discarding the SLSCopySpacesForWindows result that refreshWindows correctly fetched (StateManager.swift:1114-1132, overridden at 1135-1137). It re-runs on every poll (updateWindowFromPoll :1369-1372), every AX move/resiz
SYMPTOM: On any multi-space-per-display workflow, switching spaces then pressing any hotkey strips the previous space's tiling assignments and piles those windows into one cell of the new space; new windows ca
FIX: Only derive space from display when SLS returns empty/failed, or restrict the geometric override to windows currently on-screen (isHidden == false); never overwrite a non-empty SLS space list for an o
WHY-SUSPECTED: code=suspected ev=confirmed

### [low/SUSPECTED] restoreFocusForSpace can AX-raise a window that moved to another space, yanking the user off the space they just switched to
LOC: grid-server/Sources/GridServer/StateManager.swift:1830
MECH: On space switch, StateManager.handleSpaceChanged calls restoreFocusForSpace(newSpaceID) (StateManager.swift:1794-1796). restoreFocusForSpace (:1830-1860) takes space.lastFocusedWindowID and issues manipulator.focusWindow (:1852-1853) after checking only that the window exists in state and is not hidden/minimized (:1838-1841). It never checks window.spaces still contains spaceID. lastFocusedWindowI
SYMPTOM: Switching to a space immediately bounces you to a different space (where a previously-focused window now lives), or focuses a window the user did not expect.
FIX: Verify window.spaces.contains(spaceID) (and tileability) before restoring, and clear/refresh space.lastFocusedWindowID when a window's space assignment changes.
WHY-SUSPECTED: code=suspected ev=suspected

### [low/SUSPECTED] Poll snapshot TOCTOU resurrects destroyed windows and routes phantom windowCreated events
LOC: grid-server/Sources/GridServer/StateManager.swift:1278
MECH: The 3s poll timer captures CGWindowListCopyWindowInfo OFF the actor (StateManager.swift:1240-1245), then hops onto the actor via Task { await self?.applyPollResults(windowList) } (:1246-1248). Between snapshot and apply, an AX windowDestroyed can be processed (handleWindowDestroyed, :1525, removes the window from state.windows). applyPollResults then finds the stale wid in the snapshot, the lookup
SYMPTOM: Closed window briefly reappears in grid state and cell assignments; back-to-back ev.win.create(src=poll) then ev.win.destroy(src=poll) for the same wid ~3s apart; transient wrong window counts in cell
FIX: Keep a short-lived tombstone set of recently destroyed wids (with destroy timestamp) and skip poll re-adds whose snapshot predates the destroy.
WHY-SUSPECTED: code=confirmed ev=refuted

### [low/SUSPECTED] AX properties are only re-queried while role is nil — window misclassified at creation (e.g. transient AXUnknown subrole) stays rejected forever
LOC: grid-server/Sources/GridServer/StateManager.swift:1355
MECH: updateWindowFromPoll re-queries getAXProperties only under 'if window.role == nil' (StateManager.swift:1355-1367); refreshAXPropertiesForActiveSpace has the same guard (:992). A window that returned role="AXWindow" but a transient subrole ("AXUnknown" during app startup) at creation time keeps that cached subrole permanently. Downstream, isTileable rejects any non-empty subrole != "AXStandardWindo
SYMPTOM: Occasionally an app's main window never gets tiled (floats untouched, no border) even though it looks completely normal, and stays that way until the window is closed and reopened.
FIX: In updateWindowFromPoll, also re-query AX properties when subrole is nil/"AXUnknown" (or when the window is in the rejected set), not only when role is nil.
WHY-SUSPECTED: code=suspected ev=suspected

### [low/SUSPECTED] Display-reconnect reapply runs against a state snapshot taken before macOS finishes migrating spaces back
LOC: grid-server/Sources/GridServer/Grid/GridReconciler.swift:1089
MECH: On reconnect, StateManager.handleDisplayConfigurationChanged refreshes state and routes .displayConnected; StateManager.handleDisplayConnected (StateManager.swift:1705-1709) refreshes again, THEN GridReconciler.handleDisplayConnected sleeps 500ms 'to let macOS stabilize' (GridReconciler.swift:1084) and calls gridApply.refreshAllDisplays(displayFilter:) (:1089). But nothing re-refreshes StateManage
SYMPTOM: After replugging an external display, its windows stay on the laptop screen / are not snapped back into their cells; layout refresh appears to run but does nothing for the reconnected display.
FIX: Re-trigger a StateManager refresh (displays + spaces) after the 500ms settle delay and before refreshAllDisplays, or retry the reapply when a later reconfiguration notification shows the display's cur
WHY-SUSPECTED: code=suspected ev=confirmed

### [low/SUSPECTED] refreshDisplays joins SLS display order to NSScreen.screens by array index — frames/scale can attach to the wrong display UUID
LOC: grid-server/Sources/GridServer/StateManager.swift:404
MECH: refreshDisplays enumerates SLSCopyManagedDisplays output and passes the loop index as screenIndex (StateManager.swift:404-413) to DisplayInfoHelper.enrichDisplayInfo, which does 'let screen = screens[screenIndex]' over NSScreen.screens (DisplayInfo.swift:70-75). The two arrays come from independent subsystems (SkyLight managed-display order vs AppKit screen order, where only screens[0]=primary is 
SYMPTOM: Tiled windows on one monitor sized/positioned as if for the other monitor (wrong resolution/menu-bar inset), especially with 2+ externals or right after arrangement changes.
FIX: Match displays by UUID: map each SLS UUID to the NSScreen whose CGDisplayCreateUUIDFromDisplayID(screen.displayID) equals it, instead of joining by index.
WHY-SUSPECTED: code=suspected ev=suspected

REFUTED: ax_orphan prune evicts live panel-style windows (hit the GridNotify panel, emptying the 'notify' cell) @ grid-server/Sources/GridServer/Grid/StateValidator.swift:269 — Re-read grid-server/Sources/GridServer/Grid/StateValidator.swift in full — the cited code at :233-277, :282-307, :252, :97, :61, :81 all match the finding's quotes, and git log shows no recent change to the prune logic. But the claimed MECHANISM is wrong. (1) The GridNotify panel is not an NSPanel/n
grid-server/Sources/GridServer/StateManager.swift:1834 — restoreFocusForSpace bails silently when the space has no lastFocusedWindowID, or when the window is missing/hidden/minimized (second guard at line 1840) — 122  → win.focus.restore.skip with {sid, wid (or 0), reason: no_last_focused | window_gone | hidden_or_minimized}
grid-server/Sources/GridServer/Grid/GridFocus.swift:359 — focusCellByID discards focusWindowByID's returned actual wid; when accept-reality returned a different window, GridState focus is set to the requested index wit → focus.cell.diverged with {requested, actual, cell, spaceID} whenever the returned wid != requested
grid-server/Sources/GridServer/WorkspaceObserver.swift:211 — applicationActivated's getFocusedWindowID returns nil silently on AX failure, routing focusChanged with windowID nil which both StateManager and GridReconciler  → ax.fail with {op: app_activated_focus_query, pid, app, axErr}
grid-server/Sources/GridServer/StateManager.swift:1776 — handleSpaceChanged's fallback branch (no display's currentSpaceID changed — e.g. notification arrived after a bounce-back, or simultaneous multi-display change  → spc.changed.nodiff with {displays: [{uuid, sid}], activeSpaceID}
grid-server/Sources/GridServer/Grid/GridReconciler.swift:878 — handleFocusChanged for a window with no cell in GridState (untracked/floating window) silently skips the GridState focus update but still repaints borders; ther → reconcile.focus.untracked with {wid, spaceID, displayUUID}
grid-server/Sources/GridServer/Grid/GridReconciler.swift:442 — All windowCreated/windowDestroyed/windowMoved/minimized events arriving while suppressionDepth > 0 are dropped with no log whatsoever → ev=reconcile.event.suppressed with event kind, wid, suppressionDepth, and the active action label — this is the only way
grid-server/Sources/GridServer/StateManager.swift:952 — deriveSpaceFromDisplay silently overwrites a non-empty SLS-reported space list with the display's current space → ev=warn.space.derive_override when originalSpaces is non-empty and does not contain the derived space: wid, originalSpac
grid-server/Sources/GridServer/Grid/GridReconciler.swift:1420 — sweepDisplacedWindows silently skips windows when resolveDisplacedTarget returns nil (ambiguous multi-space membership or untracked target space) → ev=reconcile.lift.skip with wid, trackedSpaceID, actualSpaces, and skip reason (ambiguous vs untracked vs empty)
grid-server/Sources/GridServer/Grid/GridReconciler.swift:662 — Cross-space locked-cell assignment when findDisplayUUIDForSpace returns nil: window is assigned in GridState but applyCellLayout is skipped, yet reconcile.win.c → ev=warn.locked.placement_skipped with wid, lockedCell, otherSpaceID when displayUUID is nil, so the never-placed locked 
grid-server/Sources/GridServer/StateManager.swift:560 — getAXProperties returns empty AXWindowProperties (nil role) when AXUIElementCopyAttributeValue for kAXWindowsAttribute fails or no element matches — silent, so  → ev=ax.props.miss with pid, wid, windowsResult error code, and ax window count, sampled/throttled, so nil-role rejections
grid-server/Sources/GridServer/Grid/GridReconciler.swift:1054 — handleWindowMinimized's removeWindow against the focused space is a silent no-op when the window is tracked in a different space → ev=warn.min.space_mismatch when findSpaceContaining(windowID) != currentSpaceID at minimize time: wid, trackedSpace, cur
grid-server/Sources/GridServer/Grid/GridReconciler.swift:442 — Non-focus events (windowCreated, windowDestroyed, minimized, display events) arriving while suppressReconciliation is true are dropped with no log — only focusC → reconcile.suppressed.drop with event name, wid/spaceID, current suppressionDepth, and the label of the active action — w
grid-server/Sources/GridServer/StateManager.swift:1210 — addApplicationObserver silently overwrites an existing ApplicationObserver for the same pid, releasing it without stopObserving — the duplicate-observer/danglin → ax.observer.replace with pid, app name, and whether an existing entry was overwritten (and ideally stop the replaced obs
grid-server/Sources/GridServer/Borders/SimpleBorderManager.swift:147 — setCellAssignmentsImpl's isUpdating reentrancy guard returns silently, discarding an entire border sync (assignments, focus, stack modes) — same pattern at line → bdr.drop.reentrant with source, displayUUID, and assignment count so silently-stale borders can be distinguished from ne
grid-server/Sources/GridServer/Grid/GridState.swift:118 — load() wholesale replaces spaces/displaySpaces even when in-memory state already has mutations (startup race, finding #10) — the clobber is invisible. → grid.state.load with counts of in-memory spaces/windows being discarded when non-empty at load time (warn level if anyth
grid-server/Sources/GridServer/StateManager.swift:1281 — addWindowFromPoll re-adding a wid that was destroyed after the CGWindowList snapshot (poll resurrection, finding #5) is indistinguishable from a genuinely new p → win.poll.readd (or a flag on ev.win.create src=poll) when the wid was removed from state within the last poll interval —
grid-server/Sources/GridServer/Grid/GridReconciler.swift:442 — Window lifecycle events (windowCreated/windowDestroyed/windowMinimized/windowMoved/windowDeminimized) dropped because suppressReconciliation is true — currently → reconcile.suppressed.drop with event name, wid, suppressionDepth, and the label of the active action session
grid-server/Sources/GridServer/Grid/GridReconciler.swift:179 — releaseFence removing a fence that a second in-flight move still depends on (or releasing a wid with no entry) — indistinguishable from normal release in the cu → fence.release should include whether an entry existed and its remaining-time; with refcounting, log depth so overlapping
grid-server/Sources/GridServer/Grid/GridReconciler.swift:360 — focusSweep correcting focus while the osWindowID or gridFocusedWID is currently fenced — exactly the case where the sweep is fighting an in-flight move — logs t → add fencedOs/fencedGrid booleans (and suppressionDepth) to the sweep.correct payload
grid-server/Sources/GridServer/StateManager.swift:1283 — Windows that exist in StateManager state but are tileable and assigned to no GridState cell (the terminal state of a create dropped during suppression) are neve → periodic (validator-cadence) log of tileable, non-rejected, unassigned wids, e.g. validate.win.untracked with wid/app/sp
grid-server/Sources/GridServer/WindowManipulator.swift:78 — Sole-AX-window fallback in getAXElement fires silently — when the queried window ID is not in the AX list but the app has exactly one AX window, the substitute  → ax.fallback event with pid, queried wid, the sole AX window's resolved CG id, and the op being performed
grid-server/Sources/GridServer/StateManager.swift:1243 — Poll timer body: `guard let windowList = CGWindowListCopyWindowInfo(...) else { return }` — if the window-list query starts failing persistently, the 3s reconci → warn.poll event (rate-limited) when CGWindowListCopyWindowInfo returns nil, with consecutive-failure count
grid-server/Sources/GridServer/SocketServer.swift:244 — sendMessage only checks `sent < 0`; a short write (0 <= sent < data.count, possible for large state dumps on an interrupted blocking send) silently truncates th → sock.err op=short_write with socket, sent, total bytes; or loop until fully written
grid-server/Sources/GridServer/StateManager.swift:560 — getAXProperties: AXUIElementCopyAttributeValue(kAXWindowsAttribute) failure returns empty AXWindowProperties with no log — windows from AX-unready apps silently → ax.fail op=windows_list with pid, wid, AXError raw value (mirroring the equivalent log that WindowManipulator.getAXEleme
grid-server/Sources/GridServer/StateManager.swift:2002 — updateFocusedWindowForApp: both guard-else-returns (AX focused-window query fails, or _AXUIElementGetWindow fails) are silent, leaving focusedWindowID stale aft → warn.focus.app event with pid and which AX step failed
grid-server/Sources/GridServer/MSSClient.swift:206 — All MSS ops except moveWindowToSpace (setWindowOpacity, setWindowLayer, setWindowSticky, focusWindow, orderWindowToFront, minimize/unminimize, space ops) return → err.mss op=no_ctx (rate-limited) naming the operation and window/space id, as moveWindowToSpace already does at :185
grid-server/Sources/GridServer/JSONLogger.swift:81 — JSONLogWriter.writeBatch catch is fully empty ('Silent fail - logging shouldn't crash the app') and the FileHandle-open failure at :71 is a bare `return` — enti → fallback write of a one-line loss notice to stderr plus a dropped-lines counter emitted on the next successful batch
grid-server/Sources/GridServer/StateManager.swift:1356 — Nil-role re-query during poll (updateWindowFromPoll) and refreshAXPropertiesForActiveSpace (:996) silently do nothing when getAXProperties keeps returning role= → debounced ax.requery event with wid, pid, attempt count when a nil-role window fails promotion repeatedly
grid-server/Sources/GridServer/Grid/GridCommandRouter.swift:804 — Nudge-mode keypress: `try? await self.gridNudge.move(...)` (804) and `try? await self.gridNudge.resize(...)` (810) swallow GridNudgeError (.noFocusedWindow / .w → nudge.err with {dir, spaceID, err} on the catch/nil path of both move and resize callbacks
grid-server/Sources/GridServer/Grid/GridReconciler.swift:642 — Locked-cell and picker-target window adoption: `try? await gridApply?.applyCellLayout(...)` at 642, 663, and 804 discards GridApplyError (noLayout/layoutNotFoun → reconcile.apply.err with {spaceID, cellID, wid, err} whenever applyCellLayout throws on these paths
grid-server/Sources/GridServer/Grid/GridReconciler.swift:807 — Picker-launched window focus: `_ = try? await gridFocus?.focusWindowByID(windowID)` — if AX focus fails the new window stays unfocused while GridState.setFocus  → reconcile.pending.focus_fail with {wid, err} when focusWindowByID throws
grid-server/Sources/GridServer/BFD/BFDManager.swift:165 — startConfigWatcher: `guard fd >= 0 else { return }` — if bfd.yaml does not exist (or open fails) at startup, config watching is permanently off and no event rec → bfd.err.watch with {path, errno} when open(O_EVTONLY) fails
grid-server/Sources/GridServer/Grid/GridFocus.swift:173 — moveFocus cross-display attempt: `} catch { // Cross-display failed — fall through to wrap-around }` — the reason cross-display focus failed (no display, getDis → focus.cross.fail with {dir, err} before falling through to wrap-around
grid-server/Sources/GridServer/Picker/ActionExecutor.swift:85 — openDirInTmux: `_ = await runProcess(tmuxPath, args: ["new-session", ...])` — if tmux session creation fails (tmux missing, bad dir), Ghostty still launches wit → pick.err.tmux with {session, dirPath} when has-session and new-session both fail
grid-server/Sources/GridServer/Grid/GridWindowMove.swift:580 — applyPartialLayout silent early-returns: layoutID empty (559) or `guard let layoutDef = config.layoutDef else { return }` (580) — after a cross-display move the → winmove.partial.skip with {spaceID, cellIDs, reason: no_layout|layout_not_found}
grid-server/Sources/GridServer/WindowManipulator.swift:401 — moveWindowToSpace MSS-unavailable SLS fallback: `return newSpace == spaceID` — a post-move verification failure on this branch returns false with no log (the MS → err.verify with {wid, expected, actual, method: "sls"} when the fallback verification fails
grid-server/Sources/GridServer/Borders/SimpleBorderManager.swift:147 — Reentrancy guards `guard !isUpdating else { return }` (setCellAssignmentsImpl:147, updateFocusImpl:266) silently drop the entire incoming assignment/focus updat → bdr.drop.reentrant with {source, display, count} when an update is discarded by the guard
grid-server/Sources/GridServer/Grid/GridReconciler.swift:442 — Non-focus events (windowCreated, windowDestroyed, windowMoved, windowMinimized, displayConnected, ...) silently discarded while suppressionDepth > 0 — only focu → reconcile.event.suppressed with event type, wid/displayUUID, current suppressionDepth, and active action label — this is
grid-server/Sources/GridServer/ApplicationObserver.swift:76 — AXObserverAddNotification failures (register_notif / register_notifs ax.fail) omit pid and appName, so the log cannot identify WHICH app lost AX observation → pid and app name on both the per-notification register_notif failure (:76-81) and the aggregate register_notifs failure 
grid-server/Sources/GridServer/Grid/StateValidator.swift:264 — Orphan-count increments (cycle 1 of 2) are silent — the first warning for an ax_orphan prune is the prune itself, so near-misses and slow accumulation across a  → validate.orphan.tick with wid, app, count, and pid whenever a tracked window is found missing from its app's AX window l
grid-server/Sources/GridServer/StateManager.swift:1205 — createObserver's fire-and-forget Task discards observe()==false with no event tying the failure to the app being permanently unobserved → ax.observer.create.failed with pid, app, and whether a retry will occur, when observe() returns false
grid-server/Sources/GridServer/Grid/GridReconciler.swift:729 — handlePendingLaunchWindow silently returns when stateProvider is nil, dropping a picker-launched window claim with no trace → reconcile.pending.skip with reason "no_stateProvider" and wid
grid-server/Sources/GridServer/MessageHandler.swift:52 — Concurrent command execution at ingress: when two requests overlap, nothing records that they ran in parallel or in what order they completed → in-flight request count at Task start/end (e.g. msg.handle with {inflight: N}) so overlapping commands for the same wind
grid-server/Sources/GridServer/Grid/GridCommandRouter.swift:880 — handleNotify silently discards cmd.action and cmd.args for every @notify command → notify.dispatch event with {action, args} and an explicit warn when an unsupported action is collapsed to toggle
grid-server/Sources/GridServer/MessageHandler.swift:1407 — buildCommand silently drops params it does not recognize (title, body, priority, source, id, purge), so RPC payload loss is invisible → warn event listing param keys present in request.params that were not encoded into the command string
grid-server/Sources/GridServer/Grid/GridReconciler.swift:134 — A leaked beginAction token leaves suppressionDepth stuck > 0 forever with no signal (reconciler silently dead) → periodic watchdog event (e.g. warn.action.stuck with {label, depth, age_ms}) when a beginAction session exceeds a sane d
grid-server/Sources/GridServer/MessageHandler.swift:1153 — borders.setCellAssignments `guard let self else { return }` exits without ever calling completion — client hangs with zero log output → error response (and msg.err event) on every early-return path that currently skips completion
grid-server/Sources/GridServer/Grid/GridCommandRouter.swift:824 — Nudge handler.start() success path never logs how many taps/sessions exist; an orphaned second CGEventTap is invisible → nudge.tap.installed / nudge.tap.stopped events with a session id, plus a warn if a handler is replaced while non-nil
grid-server/Sources/GridServer/Grid/GridApply.swift:136 — applyLayoutBody proceeding with displayBounds == .zero (unknown space/display or nil display frame) silently computes and applies degenerate placements → err.layout.zero_bounds with spaceID, resolved displayUUID, display count, and whether visibleFrame/frame were nil — befo
grid-server/Sources/GridServer/DisplayInfo.swift:71 — enrichDisplayInfo screenIndex out-of-bounds (SLS lists more displays than NSScreen.screens) returns a frame-less DisplayState with no log → warn.dsp.screen_index_oob with uuid, screenIndex, NSScreen.screens.count — this is the silent precursor to zero-bounds l
grid-server/Sources/GridServer/StateManager.swift:1931 — Geometry-only display reconfiguration (same UUID set, changed frames/visibleFrames) is fully silent after dbg.dsp.reconfig.diff — no record that cached frames c → dsp.geometry.change per display with old/new frame and visibleFrame whenever refreshDisplays alters a frame for an exist
grid-server/Sources/GridServer/Grid/GridState.swift:245 — migrateSpaceIDs with an empty or count-mismatched oldSpaceList (stale displaySpaces snapshot) silently skips or partially pairs — the exact failure mode that lo → state.space_migrate.snapshot with displayUUID, oldSpaceList, newSpaceList, and pairedCount on every wake migration, incl
grid-server/Sources/GridServer/Grid/GridReconciler.swift:1000 — Wake/connect refreshAllDisplays failures are logged only as an errorCount; which display failed and why (noDisplayBounds vs apply error) is dropped → warn.reconcile.wake.refresh.errors should include per-entry displayUUID, displayName, and error description from the Gri
grid-server/Sources/GridServer/WorkspaceObserver.swift:147 — displayReconfigured is routed with displayUUID: "" — downstream consumers and post-mortem logs cannot tell which display or what kind of change (count vs geomet → Include an NSScreen snapshot (count + per-screen frame hash) in the ws.screen / ev.dsp.reconfig event so reconfiguration
grid-server/Sources/GridServer/Borders/BorderWindow.swift:404 — Resize redraw: SLWindowContextCreate failure after SLSSetWindowShape leaves the border at alpha 0 with no log event at all (the only trace is the absence of a r → err.bdr.resize_ctx with wid, targetID, bounds, and wasVisible — fires when context recreation fails in performUpdate's r
grid-server/Sources/GridServer/Borders/SimpleBorderManager.swift:189 — Atomic setCellAssignments branch silently drops assignment-content deltas (added/removed windows in the active cell) by routing to refreshBorderPositions with n → warn.bdr.delta_dropped with display, cell, addedWids, removedWids when oldAssignments != assignments but the refresh-onl
grid-server/Sources/GridServer/Borders/BorderWindow.swift:284 — show(): SLSSetWindowAlpha and SLSOrderWindow return codes are discarded; an ordering failure against a dead targetWindowID is completely silent, so a border sho → warn.bdr.show_fail with wid, targetID, alphaErr, orderErr when either SLS call returns non-success.
grid-server/Sources/GridServer/Borders/SimpleBorderManager.swift:585 — reassignBorders tabbed retarget: BorderWindow.retarget returns Void, so the manager cannot detect (and does not log at manager level) that the active border fai → err.bdr.reassign with cell, prevFocused, newFocused when retarget fails (requires retarget to return success).
grid-server/Sources/GridServer/Borders/BorderWindow.swift:292 — hide(reason:) accepts a reason string ('pool_release', 'style_removed', 'unspecified') but never logs it — pool release/hide transitions, central to diagnosing  → bdr.hide with wid, targetID, reason, wasVisible (sampled or debug-gated given volume).
grid-server/Sources/GridServer/Grid/GridApply.swift:383 — applyCellLayout AX placement failures: the discardable failedIDs set from applyPlacementsViaAX is dropped, so single-cell applies (cell mode command, pendingLau → layout.cell.ax_partial with {sid, cellID, failed: [wid]} whenever failedIDs is non-empty
grid-server/Sources/GridServer/Grid/GridState.swift:592 — setWindowAssignments wholesale replacement: windows present in the previous cell map but absent from the new assignments vanish with no trace, which is exactly  → state.assignments.diff with {sid, added: [wid], removed: [wid], cellsDropped: [cellID]} computed against the prior cells
grid-server/Sources/GridServer/Grid/GridReconciler.swift:442 — events arriving while suppressionDepth > 0 (windowCreated/windowDestroyed/moved/minimized) return silently — there is no record of which events were dropped dur → reconcile.suppressed.drop with {event: type, wid, depth} (rate-limited for move/resize spam)
grid-server/Sources/GridServer/Grid/GridApply.swift:129 — overlapping applyLayoutBody runs for the same space are indistinguishable: layout.apply.start/done and action.start/end carry no per-invocation ID, and action.s → a monotonically increasing applyID (and sid) in layout.apply.start, layout.apply.done, and layout.apply.ax_partial so ov
grid-server/Sources/GridServer/Grid/GridReconciler.swift:921 — handleSpaceIDReassigned migrates GridState while an action may be in flight (it deliberately bypasses suppression) but never records whether suppressionDepth wa → include {suppressionDepth, activeActionLabel} in the reconcile.space.reassign event so migrations that landed inside an 
